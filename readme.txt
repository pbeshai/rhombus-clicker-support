Project Name: iClickerDriver, for old base station.

An iClicker driver that works with the old black base station.  Note: different platforms should use different native 
libraries, all of which locate in the directory "lib". Currently Windows 32, Windows 64 and Mac OS are supported.